<div class="max-w-md mx-auto">
  <div class="mb-6">
    <%= link_to "← Retour", team_path(@team), class: "text-neon-cyan hover:text-neon-pink transition-colors duration-300" %>
  </div>

  <div class="neon-border p-8">
    <div class="text-center mb-6">
      <div class="text-5xl mb-4 pulse-glow inline-block" style="color: #bf00ff;">✏️</div>
      <h1 class="font-display text-2xl font-bold text-neon-purple neon-text-subtle">Modifier l'équipe</h1>
    </div>
    
    <%= render "form", team: @team %>
  </div>
</div>
