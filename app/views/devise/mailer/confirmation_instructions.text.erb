Bonjour <%= @resource.respond_to?(:name) && @resource.name.present? ? @resource.name : @email %>,

Bienvenue sur HitGuessr.

Pour activer votre compte, confirmez votre adresse email en ouvrant le lien suivant :
<%= confirmation_url(@resource, confirmation_token: @token) %>

A bientot sur HitGuessr,
L'equipe HitGuessr
