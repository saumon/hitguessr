Bonjour <%= @resource.respond_to?(:name) && @resource.name.present? ? @resource.name : @resource.email %>,

Nous vous confirmons que le mot de passe de votre compte HitGuessr a ete modifie.

Si vous n'etes pas a l'origine de cette action, reinitialisez votre mot de passe sans attendre.

A bientot sur HitGuessr,
L'equipe HitGuessr
